<Window x:Class="AntiDupApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AntiDupApp"
        mc:Ignorable="d"
        Title="Duplicate Files Finder" Height="606" Width="985" Icon="/icon.ico">

    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="149*"/>
            <ColumnDefinition Width="836*"/>
        </Grid.ColumnDefinitions>

        <Button Click="Button_Click" Content="🔍 Select Directory " HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="118" Height="29" />

        <ListBox Margin="5,44,5,5"
                 ItemsSource="{Binding WorkFiles}" MouseDoubleClick="ListBox_MouseDoubleClick" d:ItemsSource="{d:SampleData ItemCount=5}" Grid.ColumnSpan="2" >

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="#ebebeb"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsTextDimmed}" Value="False">
                            <Setter Property="Background" Value="Transparent"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Button Content=" Move to trash " Command="{Binding ListItemButtonCommand}" 
                                    Visibility="{Binding IsButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <TextBlock Text=" -- " />
                        <TextBlock Text="{Binding FileDateString}" />
                        <TextBlock Text=" -- " />
                        <TextBlock Text="{Binding FileSize}" />
                        <TextBlock Text=" -- " />
                        <TextBlock Text="{Binding FileName}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>
    </Grid>
</Window>
