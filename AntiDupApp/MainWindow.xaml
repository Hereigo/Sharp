<Window x:Class="AntiDupApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AntiDupApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ListBox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5,44,5,5"
                 ItemsSource="{Binding WorkFiles}" MouseDoubleClick="ListBox_MouseDoubleClick" d:ItemsSource="{d:SampleData ItemCount=5}" >

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Button Content=" [ x ] " Command="{Binding ListItemButtonCommand}" 
                                Visibility="{Binding IsButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                        
                        <TextBlock Text=" - " />
                        <TextBlock Text="{Binding FileDateString}" />
                        <TextBlock Text=" | " />
                        <TextBlock Text="{Binding FileSize}" />
                        <TextBlock Text=" | " />
                        <TextBlock Text="{Binding FileName}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

        <Button Click="Button_Click" Content="Browse..." HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="65" />

    </Grid>
</Window>
